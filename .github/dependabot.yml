version: 2
updates:
  - package-ecosystem: "composer"
    directory: "/"
    schedule:
      interval: "daily"
    # Disable regular version updates. Security updates are unaffected by this limit and remain enabled (they have their own internal limit).
    open-pull-requests-limit: 0
    versioning-strategy: increase
